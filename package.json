{
  "name": "etch-a-sketch",
  "version": "1.0.0",
  "private": true,
  "description": "A vanilla JavaScript Etch-a-Sketch for The Odin Project.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4173 --strictPort",
    "build": "vite build",
    "test": "node --test tests/*.test.js",
    "preview": "vite preview --host 0.0.0.0 --port 4173 --strictPort"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "vite": "^7.1.0"
  }
}
